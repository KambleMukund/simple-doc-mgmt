import React from 'react';

const Home = () => {
  return (
    <div
      id="home-page"
      style={{
        display: 'flex',
        alignItems: 'center',
        flexDirection: 'column',
        width: '990px',
        justifyContent: 'center',
      }}
    >
      <h2>Welcome Home !!</h2>
    </div>
  );
};

export default Home;
